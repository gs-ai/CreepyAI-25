# CreepyAI Configuration System

This directory contains the consolidated configuration system for CreepyAI. It merges the previous `config` and `configs` directories into a single, organized structure.

## Directory Structure

